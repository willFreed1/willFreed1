import 'package:flutter/material.dart';

class FormStructure extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: Text('Forming Structures'),
      centerTitle: true,
      backgroundColor: Colors.green,
    ),
  );
}